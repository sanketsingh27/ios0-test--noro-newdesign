import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Blank Native iOS App")
                .font(.title)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
